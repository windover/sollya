#NAME $AND
#QUICK_DESCRIPTION boolean AND operator
#CALLING <expr1> $COMMAND <expr2>
#TYPE ($BOOLEAN_TYPE, $BOOLEAN_TYPE) -> $BOOLEAN_TYPE
#PARAMETERS <expr1> and <expr2> represent boolean expressions


#DESCRIPTION 
$COMMAND evaluates to the boolean AND of the two
boolean expressions <expr1> and <expr2>. $COMMAND evaluates to 
true iff both <expr1> and <expr2> evaluate to true.

#EXAMPLE
true && false;

#EXAMPLE
(1 == exp(0)) && (0 == log(1));

#SEEALSO $OR
#SEEALSO $NOT

