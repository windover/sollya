#NAME $MID
#QUICK_DESCRIPTION gives the middle of an interval.

#CALLING $COMMAND(<I>)
#TYPE $RANGE_TYPE -> $CONSTANT_TYPE

#CALLING $COMMAND(<x>)
#TYPE $CONSTANT_TYPE -> $CONSTANT_TYPE

#LIBRARYNAME sollya_obj_t sollya_lib_mid(sollya_obj_t)

#PARAMETERS <I> is an interval.
#PARAMETERS <x> is a real number.

#DESCRIPTION 
Returns the middle of the interval <I>. If the middle is not exactly
representable at the current precision, the value is returned as an
unevaluated expression.

#DESCRIPTION
When called on a real number <x>, $COMMAND behaves like the identity.

#EXAMPLE
mid([1;3]);
mid(17);

#SEEALSO $INF
#SEEALSO $SUP
